<Page x:Class="Display.Views.ActorInfoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:Display.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:data="using:Display.Data"
      mc:Ignorable="d">

    <Grid>
        <controls:VideoCoverDisplay x:Name="videoControl"
                                   Click="{x:Bind OnClicked}"
                                   VideoPlayClick="{x:Bind VideoPlay_Click}"
                                   SingleVideoPlayClick="SingleVideoPlayClick"
                                   IsShowFailListView="False"
                                   IsShowSearchListView="True"/>

    </Grid>
</Page>
