<Page x:Class="Display.ContentsPage.DetailInfo.SelectVideoToPlay"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:data="using:Display.Data"
      mc:Ignorable="d">

    <StackPanel Spacing="5"
                HorizontalAlignment="Stretch">
        <TextBlock Text="多文件视频，播放选择哪一集？" />
        <ListView x:Name="ContentListView"
                  SelectionMode="Multiple"
                  ItemsSource="{x:Bind pickCodeInfoList}"
                  IsItemClickEnabled="True">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:Datum">
                    <TextBlock Text="{x:Bind Name}"
                               TextWrapping="Wrap" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
